#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0) -> sync_with_stdio(0);
    int d,k,n,b;
    cin >> d >> k;
    for (int i=0;i<d;i++) {
        cin >> n >> b;
        
    }
    return 0;
}